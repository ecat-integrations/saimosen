# 此文件为ecat集成的配置文件，无此文件则下面均为无。

requires_core: ^3.1.0
dependencies:
  - artifactId: integration-modbus
    groupId: com.ecat
    version: ^3.0.0
  - artifactId: integration-serial
    groupId: com.ecat
    version: ^3.0.0

